import Vue from "vue";
import Router from "vue-router";





let onlyAdmin = [1];
let allUser = [1, 2];
let onlyModerator = [2];

Vue.use(Router);
export default new Router({
    mode: "history",
    routes: [
      
    ]
});

